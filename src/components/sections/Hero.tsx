import useLocale from "@/components/hooks/locale";

export default function HeroSection() {
    // const {t} = useLocale();

    return (
        <div>
            <h1>
                HERO
            </h1>
            <div>
                TODO Hero
            </div>
        </div>
    )
}
